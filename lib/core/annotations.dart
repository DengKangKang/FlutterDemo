const Object autowrite = Autowrite();
const Object inject = Inject();
const Object injector = Injector();

class Injector {
  const Injector();
}

class Inject {
  const Inject();
}

class Autowrite {
  const Autowrite();
}

//网络检查
const Object requestNetwork = RequestNetwork();

class RequestNetwork {
  const RequestNetwork();
}
